Release-readiness independent review: backend, environment configuration, deployment,
documentation, performance, frontend minimum bar.
Date: 2026-08-11

BACKEND: PASS (with WARNING)
- Both apps/auth-server and apps/admin-panel have lib/logger.ts (structured JSON to stderr) +
  instrumentation.ts (onRequestError hook). auth-server: lib/logger.ts:12-27, instrumentation.ts:10-20.
- Every route.ts checked (organizations, devices, platform-admins, organization-members,
  oauth-clients, applications/sync, devices/revoke, invitations) catches errors and returns
  {error: error.message} with an appropriate status code, never a raw stack trace to the client
  (stack only goes to server-side logError).
- oauth-clients/route.ts:60-82 (callGotrueAdmin) wraps the fetch() to GoTrue in try/catch, so a
  network failure returns 502 instead of an uncaught throw.
- WARNING: no request has an explicit timeout/AbortController anywhere (grepped, zero hits tied to
  any fetch()). findUserByEmail, callGotrueAdmin, resolveEmails()'s admin.auth.admin.listUsers() loop
  all call GoTrue's admin API with no timeout — a hung/slow GoTrue instance would hang the route
  handler indefinitely, bounded only by the hosting platform's own request timeout.

ENVIRONMENT CONFIGURATION: WARNING
- auth-server: every process.env.* read is documented in apps/auth-server/.env.example. Clean match.
- admin-panel: NEXT_PUBLIC_OAUTH_CLIENT_ID read at dashboard-shell.tsx:238 and
  oauth/callback/page.tsx:35 but absent from apps/admin-panel/.env.example. Documented in
  docker/.env.example:33-38 and correctly generated by the CLI installer
  (packages/cli/src/steps/deployment.ts:303-310), so installer/docker-compose installs never hit
  this gap; only a standalone `pnpm dev` off the per-app .env.example would miss it.
- docker/docker-compose.yml:41 — GOTRUE_MAILER_AUTOCONFIRM defaults to true with only a code comment
  warning against production use; docker/.env.example never overrides it. Self-aware but not enforced.
- No hardcoded permissive CORS: corsHeaders() only fires when NEXT_PUBLIC_ADMIN_PANEL_URL is set,
  scoped to that exact origin, never '*'.
- No NODE_ENV conditionals/dev-only branches found anywhere in apps/ (grepped, zero matches).
- .env/.env.local/.env.*.local correctly gitignored; only .env.example tracked.

DEPLOYMENT: WARNING
- packages/cli/src/steps/deployment.ts generates production env correctly per target
  (Vercel/Railway/Render/Coolify/manual/Docker), not dev defaults; validateDeployUrl (lines 19-26)
  rejects http:// for non-localhost domains.
- Vercel path creates+deploys real projects via API with generated env attached.
- docker/apps.Dockerfile sets ENV NODE_ENV=production in the runner stage (line 26).
- WARNING: no health-check endpoint anywhere in apps/auth-server or apps/admin-panel (zero matches
  for "health"). docker-compose.yml only has a healthcheck: block for the db service — auth-server
  and admin-panel services have none, so container orchestrators can't verify these apps are actually
  serving before routing traffic.
- turbo.json / root package.json build/lint/typecheck/test scripts match current app/package
  structure, no stale paths found.

DOCUMENTATION: PASS
- Root README.md accurately describes actual setup (npx create-kontrolia-auth, pnpm workspace
  commands, monorepo layout, all 4+1 CLI subcommands).
- apps/documentation getting-started page correctly documents docker/.env setup, kontrolia-db-migrate,
  and env var placement consistent with what the CLI actually writes.
- Deployment procedure documented per-target inside the CLI itself (deployment.ts:28-58 NEXT_STEPS
  map), shown live during `deploy`.

PERFORMANCE: PASS
- Real range-based pagination (.range(offset, offset+PAGE_SIZE-1)) with hasMore computed from
  page-size equality confirmed on platform-admins/route.ts (page 50), organization-members/route.ts
  (page 100), admin-panel audit-logs/page.tsx (page 50).
- No unbounded list endpoint found among those checked.

FRONTEND minimum bar: WARNING
- admin-panel/audit-logs/page.tsx handles error/empty/load-more states correctly.
- admin-panel/users/page.tsx mutation handlers (remove/toggle-status/role-change) correctly
  try/catch and surface errors via setError.
- FINDING: users/page.tsx's initial list load (loadMembers, called from useEffect) does NOT check
  response.ok for error display — on failure it just returns silently with no setError call and no
  loading indicator while members === null. A real auth/network failure on first load renders
  identically to "org has zero users" and never resolves. Same failure-mode class as the already-
  tracked PQ-UX-010 (4 other pages), one additional occurrence not in that finding's original list.

SUMMARY TABLE:
Backend: PASS (WARNING: no fetch timeouts)
Environment Configuration: WARNING (one undocumented per-app env var; mailer autoconfirm default)
Deployment: WARNING (no health-check endpoints; see also migration-review.txt's CLI-migrate-
  confirmation gap)
Documentation: PASS
Performance: PASS
Frontend (minimum bar): WARNING (users/page.tsx silent failure on initial load)

No FAIL or BLOCKER findings in this scope.
