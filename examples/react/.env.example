# Vite exposes env vars prefixed VITE_ to the client (equivalent to Next's
# NEXT_PUBLIC_ prefix) — same KontrolIA installation as the other examples.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=replace-with-anon-key
