# Ejemplo: React (Vite)

Planeado para v1.5 del roadmap — ver [`examples/nextjs`](../nextjs) como referencia de integración mientras tanto (mismo `@kontrolia/react`, sin el middleware de `@kontrolia/next`).
