---
"create-kontrolia-auth": patch
---

La creación automática de proyectos en Vercel ahora también dispara el primer despliegue de cada proyecto (antes solo conectaba el repo y dejaba el build pendiente de un click manual en el dashboard). También se añadió una nota explicando paso a paso cómo generar el API token en vercel.com/account/tokens, para usuarios sin experiencia técnica.
